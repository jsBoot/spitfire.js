user-roxee-box-Linux:
    DEPLOY_ROOT: '/home/roxee/www/static/lib/webitup'
    # DOC_ROOT: '/home/roxee/doc/spitfire'
    LINKS:
        STATIC_YAML: 'https://static.roxee.net/static.yaml'

# Integration
user-jenkins-box-Linux:
    # ROOT: '/opt/puke/jenkins-roxee'
    DEPLOY_ROOT: '/var/www/deploy/static/lib/webitup'
    # DOC_ROOT: '/var/www/deploy/static/doc/spitfire'
    LINKS:
        STATIC_YAML: 'https://static.sn.ackitup.net/static.yaml'

# Raildecom workin
user-raildecom-box-Darwin:
    # ROOT: '${HOME}/workspace/webitup/buildd'
    DEPLOY_ROOT: '${HOME}/workspace/webitup/deploy/static/lib/webitup'
#    DOC_ROOT: '${HOME}/workspace/webitup/deploy/web/app/doc/roxeecore'
    LINKS:
        STATIC_YAML: 'https://static.sn.ackitup.net/static.yaml'

# Mangled Deutz workin
user-dmp-box-Darwin:
    ROOT: '${HOME}/buildd/stage.webitup.org'
    DEPLOY_ROOT: '${HOME}/buildd/deploy.webitup.org/web/static/lib/webitup'
    # DOC_ROOT: '${HOME}/buildd/deploy.webitup.org/doc/roxeecore'
    # REMOTE_BUILD: 'http:'
    LINKS:
        STATIC_YAML: 'http://static.loft.sn.ackitup.net:4242/static.yaml'

# Base setup for everyone
yak:
    ############ Package basic informations
    # Code name
    PACKAGE:
        NAME: spitfire.js
    # Version to be replaced in some files, and that might be used in paths
        VERSION: '0.1'
    # License to be replaced in files
        LICENSE: '<a href="http://en.wikipedia.org/wiki/MIT_License">MIT</a>'
    # Copyright to be replaced in files
        COPYRIGHT: 'All rights reserved <a href="http://www.webitup.fr">Web It Up</a>'
    # Author
        AUTHOR: '<a href="mailto:dev@webitup.fr">Mangled Deutz & Raildecom</a>'
    # Repo base url
        GIT_ROOT: 'https://github.com/webitup/spitfire.js/blob/master/src'
    # Link to the product page
        HOME: 'http://h.ackitup.org/products/spitfire'


    ############ Platform dependent stuff: change according to your milage
    # ROOT: '${HOME}/buildd'
    ROOT: './build'

    # Instruct puke to copy trunk versions of the shims - only of use for me:
    # VARIANT: "bleed"
    # Anything else is regular shit
    VARIANT: "bleednot"
    # All these will be exposed as Yak.NAME_ROOT, where NAME is the key
      # Note that, if relative, they will be resolved against Yak.ROOT
    # Example: Yak.BUILD_ROOT <- ${HOME}/buildd/dist
    ROOT_PATHS:
        BUILD: 'build'
        TMP: 'tmp'
        DEPLOY: 'dist'
        # Where to deploy the documentation, if any
        DOC: 'doc'

    LINKS:
        # Where to find "external" dependencies that are to be fetched either runtime or compile time
        STATIC_YAML: 'https://static.roxee.net/static.yaml'
